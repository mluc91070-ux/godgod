# Shell scripts run inside Linux containers. A CRLF checkout on Windows would
# make `#!/bin/sh` unfindable at runtime, so these stay LF everywhere.
*.sh text eol=lf
Dockerfile text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
